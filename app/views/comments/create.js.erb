$('errors').html('<%= render 'shared/errors', resource: @comment %>');
<% if @comment.persisted? %>
   $('#<%= @comment.commentable_type.downcase %>-<%= @comment.commentable_id%> .new-comment').before('<%= j render @comment %> ');
   $('#<%= @comment.commentable_type.downcase %>-<%= @comment.commentable_id%> .new-comment #comment_body').val('')
<% end %>
