.attached-file id="file-#{file.id}"
  p= link_to file.filename.to_s, url_for(file)
  -if current_user&.author_of?(file.record)
    p= link_to 'Delete file', attachment_path(file), remote: true, method: :delete
